"""
src/modules/probes
Run linear probes (logistic, SVM, diffmean) across all
model × readout × representation × layer × factor × kind × split combinations
and write layer_probe_scores.csv + transfer_scores.csv.
"""
