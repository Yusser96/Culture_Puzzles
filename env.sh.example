# Copy to env.sh and fill in. `source env.sh` before running the pipeline.
# env.sh is gitignored — never commit real keys.
export OPENAI_API_KEY="sk-..."   # used by 06 / LLM-judge steps
export HF_TOKEN="hf_..."          # required by 02 (facebook/flores is a gated dataset)
